🧩dio_client.dart

Your main HTTP client manager.

Initialize and configure Dio:

*Base URL
Headers
Timeouts

*Add interceptors:
AuthInterceptor
LoggerInterceptor

*Expose reusable methods:
get(), post(), put(), delete()
Handle exceptions (convert Dio errors into clean app errors)

✅Centralizes all HTTP logic
✅Uses interceptors for auth and logging
✅Provides a reusable instance through Riverpod
✅Handles errors cleanly