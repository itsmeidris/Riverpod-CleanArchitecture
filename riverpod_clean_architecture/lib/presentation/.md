Responsible for UI only.

Contains:

screens/ → Full pages (e.g., UserScreen).

widgets/ → UI components (like cards, list tiles, etc.).

viewmodels or providers/ (optional) → For mapping domain data to UI-friendly models.

The UI listens to state changes from the application layer (via Riverpod or another state management system).
