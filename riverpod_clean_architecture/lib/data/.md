🧱 DATA LAYER — Overview

Implements the “real world” logic — API, cache, databases, etc.
No UI logic here, just data fetching and transformation.

Contains:

datasources/ → classes that communicate with API (remote) and cache (local).
//Example: UserRemoteDataSource, UserLocalDataSource.

models/ → JSON models, used only inside data layer.
//Example: UserModel with fromJson/toJson.

repositories/ → concrete implementation of the domain’s repository interface.
//Example: UserRepositoryImpl (calls data sources, handles caching logic).
