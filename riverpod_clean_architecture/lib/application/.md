🔹 user/application/

This is the bridge between domain logic and the UI.

Contains:

controllers/ or notifiers/ → Handle state and coordinate use cases (using Riverpod, Bloc, etc.).

providers/ → Define Riverpod providers for controllers, use cases, and repositories.

This layer handles:

When to call getUserData
Whether to load from cache or API
Exposing loading/error/success states to UI

It does not render widgets or contain API code.
-----------------------------------------------

1.Provider ==> Expose a simple immutable dependency (Static value, Dio client, Repository or Use case)
*Dependency injection

2.StateProvider ==> Hold simple mutable states
(Filter toogle, pagination, like setState())
*Simple UI states

3.FutureProvider ==> Fetch asynchronous data once and expose its state
(Loading, simple data, secure storage, error)
*Data fetching

4.StateNotifierProvider ==> Manage complex states and logic, often wrapping a custom StateNotifier class
(Controllers and Viewmodels)
*UI logic (controller)
Example :  
*UserController that uses a repository to fetch and store users, updating its state (AsyncValue<List<UserEntity>>).