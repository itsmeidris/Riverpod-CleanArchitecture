import 'package:flutter/widgets.dart';

class WorkInfos extends StatelessWidget {
  const WorkInfos({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}