🧠 3. UserRepositoryImpl — The Bridge Between Data & Domain

Purpose: It connects the data sources to the domain layer.

✅ Responsibilities:

*Calls the UserRemoteDataSource to get UserModels.
*Transforms them into UserEntity objects (domain-friendly).
*Implements the interface defined in the domain layer (UserRepository).