🧩interceptors/logger_interceptor.dart
Handles logging/debugging.

Responsibilities:

*Log requests (method, URL, headers, body)
*Log responses (status code, data)
*Log errors (message, stacktrace)

Only active in debug mode
------------------------------------------

🧩interceptors/auth_interceptor.dart
Handles authorization logic.

Responsibilities:

*Add Authorization: Bearer <token> header to each *outgoing request
*Optionally refresh tokens if expired
*Skip adding tokens for public endpoints

*🧾 AuthInterceptor → adds Bearer token to every request
*🪵 LoggerInterceptor → logs all requests and responses for debugging
*🔄 RetryInterceptor → retries a request automatically if the server temporarily fails
*⚠️ ErrorInterceptor → maps server errors into app-friendly messages